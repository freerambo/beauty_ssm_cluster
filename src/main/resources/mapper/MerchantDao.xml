<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.cruiser.dao.MerchantDao">


    <update id="enableById">
        UPDATE merchants
        SET enable = 1
        WHERE merchant_id = #{merchantId}
    </update>

    <update id="disableById">
        UPDATE merchants
        SET enable = 0
        WHERE merchant_id = #{merchantId}
    </update>

    <select id="queryByMerchantCode" resultType="Merchant">
        SELECT *
        FROM merchants
        WHERE
        merchant_code = #{merchantCode}
        ORDER BY create_time desc
    </select>

    <select id="queryByMerchantName" resultType="Merchant">
        SELECT *
        FROM merchants
        WHERE
        merchant_name like concat('%', #{merchantName}, '%')
        ORDER BY create_time desc
        limit #{offset},#{limit}
    </select>

</mapper>